// Solicita uma frase ou palavra ao usuário
var texto = prompt("Digite uma frase ou palavra:");

// Converte o texto para todas as letras em minúsculas
var textoMinusculas = texto.toLowerCase();

// Exibe o texto com todas as letras em minúsculas
alert(`O texto em minúsculas é: ${textoMinusculas}`);
